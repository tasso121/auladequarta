from li1.command.SWrite import SWrite


class Write(SWrite):
    """Em li2 (Java) esta mesma classe se chama 'Write' (identica a SWrite
    de li1, apenas renomeada). Mantemos como alias/subclasse para preservar
    o nome usado pelos Exemplo*.java originais."""
